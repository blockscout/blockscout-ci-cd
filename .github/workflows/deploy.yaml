name: Test deploy


on:
  push:


jobs:
  deploy:
    - uses: convictional/trigger-workflow-and-wait@v1.6.1
      with:
        owner: blockscout
        repo: depoloyment-values
        github_token: ${{ secrets.GITHUB_TOKEN }}
