name: E2E K8s account

on:
  push:
  workflow_dispatch:
  # pull_request:

jobs:
  deploy_and_tests:
    uses: blockscout/blockscout-ci-cd/.github/workflows/e2e_new.yaml@nz-e2e-tests
    secrets: inherit
